package wujin.tourism.android.common;

public class CustomWorldHelper {
	// public static final int LIST_TYPE_EXAMPLE_1 = 1;
	// public static World sharedWorld;
	// public static World generateObjects(Context context, double latitude,
	// double longitude, ArrayList<NewArbean> newArrayList)
	// {
	// if (sharedWorld != null)
	// {
	// return sharedWorld;
	// }
	// sharedWorld = new World(context);
	// sharedWorld.setGeoPosition(Double.valueOf(String.valueOf(latitude).replace(".",".0")),Double.valueOf(String.valueOf(longitude).replace(".",".0")));
	// for (int i = 0; i < newArrayList.size(); i++)
	// {
	// GeoObject go1 = new GeoObject(new Long((long) i));
	// go1.setGeoPosition(Double.valueOf(String.valueOf(newArrayList.get(i).getLatitude()).replace(".",".0")),Double.valueOf(String.valueOf(newArrayList.get(i).getLongitude()).replace(".",".0")));
	// go1.setImageResource(R.drawable.ic_launcher);
	// go1.setName(newArrayList.get(i).getNameString());
	// sharedWorld.addBeyondarObject(go1);
	// }
	// return sharedWorld;
	// }

}
